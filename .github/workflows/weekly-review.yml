name: Weekly Project Review

on:
  schedule:
    # 매주 일요일 오전 9시(KST)
    - cron: "0 0 * * 0"

  workflow_dispatch:
    inputs:
      force_review:
        description: "새 변경사항이 없어도 강제로 다시 분석합니다."
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  weekly-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout automation repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run weekly project review
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          TARGET_GITHUB_TOKEN: ${{ secrets.TARGET_GITHUB_TOKEN }}
          TARGET_REPO: leeony2636/docker-fastapi-sentiment-api
          LOOKBACK_DAYS: "7"
          FORCE_REVIEW: ${{ inputs.force_review }}
        run: python scripts/weekly_review.py